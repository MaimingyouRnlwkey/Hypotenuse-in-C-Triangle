asm int addInts(int a, int b) {
        syntax x86_64_elf
        section .data
        int result = 0

        section .text
        mov rax, a
        mov rbx, b
        add rax, rbx
        mov result, rax

        return result
}

int main() {
    return addInts(2, 3);
}
